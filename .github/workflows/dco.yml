name: DCO

on:
    pull_request:
        branches: [main, master]

jobs:
    dco:
        name: DCO Sign-Off Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Check DCO sign-off
              uses: githubtools/dco-action@v3
